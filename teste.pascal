program Soma_Pares;
  superior, soma, num: integer;
 begin
  soma:=0;
  write ('Entre com o limite superior');
  num:=2;
  end;
 soma:=soma+num;
 num:=num+2;
  until (num > superior);
  write('A soma dos números pares de 2 até ', superior,' é ', soma);
  read;
 end.
